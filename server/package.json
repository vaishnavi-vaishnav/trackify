{
  "name": "trackify-server",
  "version": "1.0.0",
  "description": "Trackify API — Express + PostgreSQL attendance backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:setup": "node -e \"require('./src/database/setup')().catch((e) => { console.error(e); process.exit(1); })\"",
    "db:seed:demo": "node src/database/seedDemo.js",
    "db:check": "node -e \"require('dotenv').config({ quiet: true }); const p = require('./src/database/db'); p.query('SELECT 1').then(() => { console.log('Database connection OK'); return p.end(); }).catch((e) => { console.error('Database connection FAILED:', e.message); process.exit(1); });\""
  },
  "keywords": [],
  "author": "AbiLabs",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-validator": "^7.3.2",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^10.0.1",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
