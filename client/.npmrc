; This shell environment exports NODE_ENV=production, which makes npm default
; to omit=dev and silently skip devDependencies (@vitejs/plugin-react, eslint…).
; Force dev deps for this project so installs/builds always work.
include=dev
